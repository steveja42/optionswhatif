# Redirects from what the browser requests to what we serve- on netlify
#This will effectively serve the index.html instead of giving a 404 no matter what URL the browser requests - for historypushstate on Single Page Apps
/*    /index.html   200